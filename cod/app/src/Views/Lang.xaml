<Window x:Class="ShouTech.App.Views.Lang"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Language"
        Width="420"
        Height="290"
        MinWidth="380"
        MinHeight="260"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="True"
        Background="#EEEEEE"
        FontFamily="Segoe UI, Tahoma">

    <Window.Resources>
        <!-- ComboBox: language name vertically centered inside the field -->
        <Style x:Key="LanguageComboStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#666666"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Chrome"
                                                Background="#FFFFFF"
                                                BorderBrush="#666666"
                                                BorderThickness="1"
                                                CornerRadius="2"
                                                SnapsToDevicePixels="True">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="28"/>
                                                </Grid.ColumnDefinitions>
                                                <!-- empty area; content is overlaid by ContentPresenter -->
                                                <Border Grid.Column="1" Background="Transparent">
                                                    <Path HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Data="M 0 0 L 6 6 L 12 0"
                                                          Stroke="#444444"
                                                          StrokeThickness="1.5"
                                                          Fill="Transparent"/>
                                                </Border>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#1A237E"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="Chrome" Property="BorderBrush" Value="#1A237E"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- Selected item text — centered vertically in the field -->
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Margin="10,0,32,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              RecognizesAccessKey="True"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>

                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Border x:Name="DropDownBorder"
                                        Background="#FFFFFF"
                                        BorderBrush="#888888"
                                        BorderThickness="1"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <ScrollViewer Margin="1" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#999999"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#E8EAF6"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#C5CAE9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <DockPanel Margin="16" LastChildFill="True">

        <!-- Document order OK | Cancel; full-window RTL mirrors for Arabic preview -->
        <Grid x:Name="btnBar"
              DockPanel.Dock="Bottom"
              HorizontalAlignment="Right"
              Width="220"
              Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnOk"
                    Grid.Column="0"
                    Content="OK"
                    Height="34"
                    IsDefault="True"
                    Click="OnOk"
                    Background="#1A237E"
                    Foreground="#FFFFFF"
                    BorderThickness="0"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Cursor="Hand"/>
            <Button x:Name="btnCancel"
                    Grid.Column="2"
                    Content="Cancel"
                    Height="34"
                    IsCancel="True"
                    Click="OnCancel"
                    Background="#FFFFFF"
                    Foreground="#222222"
                    BorderBrush="#888888"
                    BorderThickness="1"
                    FontSize="13"
                    Cursor="Hand"/>
        </Grid>

        <Border Background="#FFFFFF"
                BorderBrush="#BDBDBD"
                BorderThickness="1"
                CornerRadius="4"
                Padding="18">
            <StackPanel>
                <TextBlock Text="ShouTech ERP"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#1A1A1A"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Please select your preferred language to continue."
                           FontSize="13"
                           Margin="0,12,0,0"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Foreground="#555555"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Preferred language"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Margin="0,20,0,6"
                           Foreground="#333333"/>

                <ComboBox x:Name="cmbLanguage"
                          Style="{StaticResource LanguageComboStyle}"
                          IsEditable="False"
                          SelectedIndex="0"
                          SelectionChanged="OnLanguageSelectionChanged">
                    <ComboBoxItem Tag="en" Content="English"/>
                    <ComboBoxItem Tag="ar" Content="العربية (Arabic)"/>
                    <ComboBoxItem Tag="fr" Content="Français (French)"/>
                    <ComboBoxItem Tag="tr" Content="Türkçe (Turkish)"/>
                </ComboBox>
            </StackPanel>
        </Border>
    </DockPanel>
</Window>
