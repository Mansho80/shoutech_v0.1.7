Restore plan and missing files
=============================

Missing or zero-length critical files found during scan:
- ShouTech.sln
- svc\svc.csproj (created placeholder)
- cod\app\auth.exe (zero)
- cod\app\lnc.exe (zero)
- cod\app\main.exe (placeholder/text)
- cod\cor\*.dll (zero)
- bin\SecEng.dll (zero)
- int\brai.py (zero)

Recommended recovery options:
1) If you have a remote repository or backup, restore full source and replace zero-length files.
2) If not, recover business logic by:
   - Recreating C# projects and reimplementing key modules (start with `cod/app/src`)
   - Rebuilding C++ engine from `sec/src` to produce `SecEng.dll` and `PROT.dll`
   - Convert `dba/mig/*.sql` into EF Core Migrations or Flyway scripts and apply to a test DB.

Notes on priority:
- High: Recover `ShouTech.sln`, all `*.csproj`, `PROT.dll` native library, and valid `main.exe`.
- Medium: Create migration pipeline and seeds.
- Low: CI/CD and containerization (after build is reproducible).
